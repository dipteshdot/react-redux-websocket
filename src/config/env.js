export const PROTOCOL = 'http';
export const DOMAIN = 'localhost'; // API Domain
export const PORT = '8080'; // API PORT

export default {
	/** API */
	API: {
		/**  API Base URL */
		URL: `${PROTOCOL}://${DOMAIN}:${PORT}`
	}
};
